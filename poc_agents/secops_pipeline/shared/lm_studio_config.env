# LM Studio Configuration for SecOps Pipeline

# LM Studio API URL - Set to host.docker.internal for Docker containers to access host
LMSTUDIO_API_URL=http://host.docker.internal:1234/v1

# Model name to use from LM Studio
LLM_MODEL_NAME=lmstudio-community/Qwen3-8B-GGUF

# Timeout in seconds (shorter timeout to avoid hanging if LM Studio is not responding)
LMSTUDIO_TIMEOUT_SECONDS=15
